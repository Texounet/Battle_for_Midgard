#include "../../header/main.h"

void select1()
{
  if(sel == 1)
    {
      sel--;
      positionSelect.y =  positionSelect.y - 100;
    }
}

void select2()
{
  if(sel == 0)
    {
      sel++;
      positionSelect.y =  positionSelect.y + 100;
    }
}

void select3(SDL_Surface *ecran)
{
  if( positionSelect.y == 250)
menuJouer(ecran);
	      else if( positionSelect.y == 350)
{
		niveauActuel = 1;
		editeur(ecran);
}
}
