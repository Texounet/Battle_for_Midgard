#include "../header/main.h"

void Coup_D_Boule(SDL_Surface *ecran, int mstr)
{

	eff_ecr(ecran, mstr);
	anim_Boule(ecran, mstr);
	C_Boule(ecran);
 	if(hero.hp > 0 && enemy.hp > 0)
	att_en(ecran, mstr);
}

void Coup_Bas(SDL_Surface *ecran, int mstr)
{
	eff_ecr(ecran, mstr);
	anim_Bas(ecran, mstr);
	C_Bas(ecran);
 	if(hero.hp > 0 && enemy.hp > 0)
	att_en(ecran, mstr);
}

void Epee(SDL_Surface *ecran, int mstr)
{
	eff_ecr(ecran, mstr);
	anim_Epee(ecran, mstr);
	Ep(ecran);
 	if(hero.hp > 0 && enemy.hp > 0)
	att_en(ecran, mstr);
}

void Coup_Tordu(SDL_Surface *ecran, int mstr)
{
	eff_ecr(ecran, mstr);
	anim_Tordu(ecran, mstr);
	C_Tordu(ecran);
 	if(hero.hp > 0 && enemy.hp > 0)
	att_en(ecran, mstr);
}